# Fallback values for the secrets-gradle-plugin when local.properties (which
# is gitignored) hasn't set a real key yet — lets the project sync/build
# without crashing, though Maps tiles won't load until a real key is set.
# See README.md for how to get an Android-restricted Maps API key.
MAPS_API_KEY=DEFAULT_API_KEY_FOR_CI
