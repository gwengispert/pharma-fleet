<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Android 11+ (API 30+) package visibility: without this, resolveActivity()/
         startActivity() against Google Maps' google.navigation:/google.streetview:
         intents (see MainActivity.openMap/openStreetView) can't see the Maps app
         even when it's installed. -->
    <queries>
        <package android:name="com.google.android.apps.maps" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.PharmaFleetDriver">

        <!-- Android-restricted key (package name + SHA-1) — distinct from the
             browser (NEXT_PUBLIC_GOOGLE_MAPS_API_KEY) and server
             (GOOGLE_MAPS_API_KEY) keys already used elsewhere in this repo.
             See README.md. Value is injected by the secrets-gradle-plugin
             from local.properties / local.defaults.properties. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MapActivity"
            android:exported="false"
            android:label="@string/app_name" />
    </application>
</manifest>
